# MARVIS Skill V2.0 账号授权与运行模式说明

> 版本：V1.0  
> 日期：2026-06-22  
> 基线：v2.0_release / v2.0 Tag

---

## 一、账号未授权状态下的可用性

### 1.1 分模块区分

| 模块 | 可用性 | 说明 |
|------|------|------|
| LUT 滤镜渲染 | ✅ 完全可用 | 本地参数运算，不涉及平台账号 |
| 分层视频生成 | ✅ 完全可用 | 本地渲染，无授权依赖 |
| 四层合规质检（final_qa） | ✅ 完全可用 | 本地逻辑校验 |
| 计价核算 | ✅ 完全可用 | 本地计算，不涉及线上交易 |
| 热点素材匹配 | ✅ 完全可用 | 本地素材库匹配 |
| 30 天定时清理 | ✅ 完全可用 | 本地文件系统操作 |
| 全量 Demo 测试 | ✅ 完全可用 | 本地模拟运行 |
| 多平台发布（platform_api） | ❌ 不可用 | 需平台账号 Token |
| 粉丝互动（评论/回复/私信） | ❌ 不可用 | 需平台账号授权 |

### 1.2 结论

即使无账号授权，MARVIS Skill V2.0 核心价值不受影响，可作为**本地素材生产、视频批量加工、调色模板调试、业务流程测试**工具正常投产使用。

---

## 二、当前阶段执行主体

### 场景 1：单机人工手动执行（当前唯一落地方式，Hermes 未上线）

**执行主体：运维 / 测试操作人员**

1. 节点拉取 `v2.0` 基线工程，本地手动启动
2. 通过 `demo_run.py` 手动传入自定义任务 JSON，本地执行渲染、调色、质检
3. 运维手动定期运行 `task_cleanup_scheduler.py` 清理过期素材
4. 配置微调后，操作人员双击 `auto_push.bat` 同步远端基线
5. 局限：无自动任务分发，大批量任务需人工分批启动，线上互动/发布暂不可用

### 场景 2：未来 Hermes / AiToEarn 落地后的自动执行

| 角色 | 组件 | 职责 |
|------|------|------|
| 业务发起方 | AiToEarn | 接收渠道/用户订单，向 Hermes 推送生产需求；接收成品素材与统计数据，完成收益结算 |
| 调度执行载体 | Hermes | 自动分发任务至多节点；全局托管定时清理、算力错峰、异常重试；统一拉取配置下发至所有节点 |
| 底层执行载体 | MARVIS Skill V2.0（v2.0 基线） | 纯执行工具，被动接收 Hermes 下发的任务参数完成视频加工；账号授权凭证由 Hermes 集中托管分发，不再由单机本地保存 |

---

## 三、账号授权缺失临时解决方案

1. **仅本地素材加工**：直接使用 MARVIS，跳过 `platform_api` 账号互动/发布模块，只跑渲染 Demo
2. **后续需启用线上互动/发布**：
   - 提前收集各平台账号授权 Token，统一存入 Hermes 配置中心（未来上线后）
   - 当前单机阶段如需测试线上功能，手动将授权凭证填入本地配置文件，测试完成后妥善保管密钥，禁止提交至 Git 远端（`.gitignore` 已屏蔽密钥类文件）

---

## 四、核心结论

1. MARVIS Skill V2.0 依旧有用，核心视频生产、调色、质检、运维能力完全不受账号授权限制
2. 当前无 Hermes / AiToEarn、无账号授权：人工操作人员是唯一执行主体，手动单机运行
3. 三层架构完整落地后：由 AiToEarn 发起订单、Hermes 统一调度、多节点 MARVIS 自动执行，账号授权由 Hermes 集中管控
