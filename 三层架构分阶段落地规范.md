# 三层架构分阶段落地现状修正与阶段执行规范

> 版本：V1.0  
> 日期：2026-06-22  
> 依赖基线：MARVIS Skill V2.0（v2.0_release / v2.0 Tag）

---

## 一、三层组件当前真实落地状态

| 架构层级 | 组件名称 | 开发部署状态 | 说明 |
| --- | --- | --- | --- |
| 业务上层 | AiToEarn | 方案规划阶段 | 仅完成接口对接规范文档，未开发、未上线 |
| 调度中台层 | Hermes | 方案设计阶段 | 调度逻辑、算力管控、定时托管、异常重试仅纸面规划，无可运行服务实例 |
| 底层执行层 | MARVIS Skill V2.0 | 已正式投产 | 完整可运行工程，v2.0 基线远端同步完成，算力节点可独立部署运行 |

---

## 二、现阶段（无 Hermes / AiToEarn）独立运行规则

### 2.1 MARVIS Skill 单机独立运行模式

当前无调度中台，仅支持单机手动/脚本驱动执行：

1. 入口固定为 `demo_run.py`，手动传入订单 JSON 参数本地执行
2. 定时清理暂使用原生 `task_cleanup_scheduler.py` 本地定时，单机独立调度，不依赖 Hermes 全局管控
3. 任务算力无自动错峰，大批量任务需人工错峰执行，避免 GPU 资源耗尽

### 2.2 配置微调流程不变

仅允许修改 `filter_lut_config.json`、清理周期常量，测试后通过 `auto_push.bat` 同步远端基线，多节点手动拉取更新配置，无 Hermes 自动同步机制。

### 2.3 临时日志管理

`.gitignore` 持续屏蔽运行临时文件，单机本地日志手动定期清理，暂无 Hermes 统一日志汇总面板。

---

## 三、后续分层落地分步规划

### 阶段 1：完成 Hermes 调度中台开发部署

1. 实现核心能力：任务队列分发、算力错峰调度、任务异常重试、全局定时任务托管
2. Hermes 接管原有单机 `task_cleanup_scheduler.py` 定时逻辑，统一全节点清理窗口
3. Hermes 增加节点配置自动拉取能力，替代手动 `git pull` 同步参数

### 阶段 2：AiToEarn 业务层对接 Hermes

1. 开发 AiToEarn 订单推送接口、任务结果回调接口
2. 打通完整链路：AiToEarn 订单下发 → Hermes 调度分配 → MARVIS 节点执行 → Hermes 汇总结果 → AiToEarn 收益结算

### 阶段 3：全三层灰度投产

单节点完整链路验证无误后，批量扩容 MARVIS 执行节点，正式全量投产。

---

## 四、当前投产边界说明

1. 现阶段仅 MARVIS V2.0 可独立交付使用，调度、业务上层均为规划文档，不具备联动调度能力
2. 所有多节点协同、全局算力管控、自动定时托管功能，需等待 Hermes 开发完成后才能启用
3. 现有《AiToEarn_Hermes_MARVIS_调度协调方案.md》为未来完整三层联动标准，当前仅作开发参考，暂不落地执行

---

## 五、节点部署指令维持不变

单机投产拉取基线命令依旧有效，用于独立运行底层执行能力：

```bash
git clone https://github.com/yuanwu6666/skill.git
cd skill
git checkout v2.0
```
